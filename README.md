To tun

install playwright library (`pip install playwright` and then `playwright install`)
Install also mss, pillow, python-dotenv, google-genai, pynput libraries
Create a .env file that contains :
GEMINI_API_KEY=YOUR_API_KEY

run main.py
